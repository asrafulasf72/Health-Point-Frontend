make a directory in the module directory which is in the components directory. the new directory name will be home. there will be PublicNavbar.tsx file that will contain our navigation for this web application and PublicFooter.tsx that will be a footer that matches our application. and the other components between this two will be Steps. tsx which will tell how can we take services from the web application and another component will be top doctors that will show atleast 3 doctors card and there will be a show all button to see the all the doctors. 
note that the home page must be responsive and the components will be called in the page.tsx of commonLayout group route. 
tell me the implementation plan first then ask me before proceed. 

Please add a hero.tsx too in the home module and place it right after the PublicNavbar.tsx. The Hero section must be look good and appropriate for the site and the whole home must maintain the theme of the site.  add this to the implimenttion plan and the other things are quite okay.


Create a floating chatbot wideget type component in the components directory. 
initially it will look like a  floating message bubble on the right bottom corner. after clicking it the user can chat with our AI Assistant and utilise the rag action from rag.action.ts there. remember the ingestdoctorsaction can only be performed by an admin or superadming. and anyone can query. so sample some query to initate the chat easily example: Nurologist in dhaka.  
We will use the component on the main layout so that anyone can see it on the UI. but remember the sync doctor data can only visible and actionable to the admin and superadmin.
so create a plan and ask me before execute.